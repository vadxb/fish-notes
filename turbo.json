{
  "$schema": "https://turborepo.org/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "env": ["JWT_SECRET"]
    },
    "dev": {
      "cache": false
    },
    "lint": {},
    "test": {}
  }
}
